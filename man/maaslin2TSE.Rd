% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maaslin2TSE.R
\name{maaslin2TSE}
\alias{maaslin2TSE}
\title{Maaslin2 Wrapper for TreeSummarizedExperiment (TSE) Objects}
\usage{
maaslin2TSE(
  tse,
  assay.type = "relative_abundance",
  outdir = "~/tempdir",
  outdir_delete = FALSE,
  min_abundance = 0,
  min_prevalence = 0.1,
  min_variance = 0,
  normalization = "TSS",
  transform = "LOG",
  analysis_method = "LM",
  max_significance = 0.25,
  random_effects = NULL,
  fixed_effects = NULL,
  correction = "BH",
  standardize = TRUE,
  cores = 1,
  plot_heatmap = TRUE,
  heatmap_first_n = 50,
  plot_scatter = TRUE,
  max_pngs = 10,
  save_scatter = FALSE,
  save_models = FALSE,
  reference = NULL
)
}
\arguments{
\item{tse}{A `TreeSummarizedExperiment` object containing microbiome data.}

\item{assay.type}{Character string specifying the name of the assay to use 
(default: "relative_abundance").}

\item{outdir}{Character string specifying the output directory for temporary 
MaAsLin2 files. Files are unlinked after execution (default: "~/tempdir").}

\item{outdir_delete}{\code{logical}. Should the output directory be deleted?
default = FALSE}

\item{min_abundance}{Numeric. The minimum abundance threshold for filtering 
features (default: 0.0).}

\item{min_prevalence}{Numeric. The minimum prevalence threshold for filtering 
features. (default: 0.1).}

\item{min_variance}{Numeric. The minimum variance threshold for filtering 
features (default: 0.0).}

\item{normalization}{Character. The normalization method to apply. 
Options: "TSS", "CLR", "CSS", "NONE", "TMM" (default: "TSS").}

\item{transform}{Character. The transformation method to apply. 
Options: "LOG", "LOGIT", "AST", "NONE" (default: "LOG").}

\item{analysis_method}{Character. The analytical method to use. 
Options: "LM", "CPLM", "ZINB", "NEGBIN" (default: "LM").}

\item{max_significance}{Numeric. The q-value threshold for significance 
(default: 0.25).}

\item{random_effects}{Character vector indicating the metadata columns to be 
used as random effects (default: NULL).}

\item{fixed_effects}{Character vector indicating the metadata columns to be 
used as fixed effects. Must be present in `colData(tse)`.}

\item{correction}{Character. Multiple hypothesis testing correction method. 
Options include "BH", "holm", etc. (default: "BH").}

\item{standardize}{Logical. Whether to standardize continuous metadata 
(default: TRUE).}

\item{cores}{Integer. Number of cores for parallel processing (default: 1).}

\item{plot_heatmap}{Logical. Whether to generate a heatmap plot (default: TRUE).}

\item{heatmap_first_n}{Integer. Number of top features to include in the heatmap 
(default: 50).}

\item{plot_scatter}{Logical. Whether to generate scatter plots for significant 
associations (default: TRUE).}

\item{max_pngs}{Integer. Maximum number of PNGs to save (default: 10).}

\item{save_scatter}{Logical. Whether to save scatter plots to disk (default: FALSE).}

\item{save_models}{Logical. Whether to save the fitted models (default: FALSE).}

\item{reference}{Character string or vector specifying the reference level(s) 
for categorical fixed effects (default: NULL).}
}
\value{
A data frame containing the merged results of MaAsLin2 statistics 
  (Beta, SE, t_stat, Confidence Intervals, P-values, and adjusted P-values) 
  alongside the feature metadata (`rowData`) and compositional statistics. 
  Results are ordered by adjusted p-value.
}
\description{
This function provides a seamless wrapper to run MaAsLin2 
directly on a `TreeSummarizedExperiment` object. It also includes the benefit
of sinking console output while running it, and deletes all temporary files 
if requested (default 'yes'). Finally, the function return a cleaned, 
formatted data frame containing the statistical results merged with 
feature metadata and some per-feature statistics (rowData).
}
\examples{
library(mia)
data(enterotype)
enterotype <- transformAssay(enterotype, 
  assay.type = "counts", 
  method = "clr", 
  pseudocount = TRUE)

no_NA_variables <- complete.cases(colData(enterotype)[,c("Enterotype","Age")])
enterotype_noNAs <- enterotype[,no_NA_variables]
DA_results <- maaslin2TSE(enterotype_noNAs, 
  assay.type = "counts",
  transform = "LOG",
  fixed_effects = c("Enterotype","Age"))

str(DA_results)
}
